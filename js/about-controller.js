// Attach another controller to our app. this is not used unless the router 
// loads it. the router will load it if the '.when' is TRUE.
navRoutes.controller('aboutController', function($scope){
	
});